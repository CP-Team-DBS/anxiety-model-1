services:
  app:
    container_name: anoto-gad7
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - anoto-network

networks:
  anoto-network:
    external: true
